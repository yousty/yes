# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in yes-read-api.gemspec
gemspec

gem 'yes-core', path: '../yes-core'

group :development, :test do
  gem 'dotenv-rails', '~> 2.8'
  gem 'factory_bot', '~> 6.0'
  gem 'pg', '~> 1.5'
  gem 'rake', '~> 13.0'
  gem 'rspec', '~> 3.0'
  gem 'rspec-rails', '~> 6.0.2'
  gem 'simplecov', '~> 0.21'
  gem 'simplecov-formatter-badge', '~> 0.1'
end

instance_eval(File.read(File.expand_path('spec/dummy/Gemfile', __dir__)))
